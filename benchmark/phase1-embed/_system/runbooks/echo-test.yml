# Echo Test Runbook
# Simple test to verify runbook execution works

runbook_id: echo-test
version: "1.0"
description: Simple echo test to verify runbook system works

globals:
  token_budget: 1000

steps:
  - id: echo
    type: echo
    inputs:
      message: "Hello from runbook!"
